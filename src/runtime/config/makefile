# generated by ./configure 
# system settings "gcc-x86_64-unknown-linux-gnu" */
PREFIX=/usr/local
BINDIR=$(PREFIX)/bin
CC=gcc
LINK=gcc
STRIP=strip
LINKOPTS= -lm -ldl
EXE=
DLL=.so
CP=cp
CD=cd
RM=rm -f
MKDIR=mkdir -p
CONFIGPATH=config
CCOPTS=-fno-defer-pop -Wall -fno-strict-aliasing -I$(CONFIGPATH)
